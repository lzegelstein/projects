#pragma once
	
#include <iterator>
#include <cmath>
#include <list>
#include <stack>

class DFS {
    public:
        DFS::DFS(std::vector<Vertex> v, Vertex n)
    private:
        std::vector<Vertex> _vertice;
}