#include "../parse.h"
#include "../cs225/catch/catch.hpp"
#include <vector>

	
	TEST_CASE("test", "")
	/*
	TEST_CASE("test", "[parse]") {
	    std::vector<std::string> file = file_to_vector("tests/parse/parsetest.csv");
	    std::vector<std::vector<std::string>> tokens = tokenizer(file);
		REQUIRE(file.size() == 4);
	    REQUIRE(tokens[0][0] == "we");
	    REQUIRE(tokens[0][1] == "are");
	    REQUIRE(tokens[0][2] == "very");
	    REQUIRE(tokens[0][3] == "screwed");
		REQUIRE(file.size() == 4);
	    REQUIRE(tokens[0].size() == 4);
	    REQUIRE(tokens[1].size() == 4);
	    REQUIRE(tokens[2].size() == 1);
	    REQUIRE(tokens[3].size() == 3);
	}
	*/
